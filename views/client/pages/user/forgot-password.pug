extends ../../layout/default.pug
include ../../mixins/alert.pug

block main
  +alert(3000)
  +alertError(3000)

  .container.my-5
    .row.justify-content-center
      .col-md-6.col-lg-5
        .card.shadow.border-0.rounded-4
          .card-body.p-4.bg-white
            h2.text-center.mb-4
              i.fa-solid.fa-unlock-keyhole.me-2.text-primary
              | Quên mật khẩu

            form(action="/user/forgot-password", method="POST", autocomplete="off")
              .mb-3
                label.form-label(for="email")
                  i.fa-solid.fa-envelope.me-2.text-info
                  | Email đăng ký tài khoản
                input#email.form-control(
                  type="email",
                  name="email",
                  required,
                  placeholder="Nhập email bạn đã đăng ký"
                )
              .mb-4
                small.text-muted
                  | Sau khi gửi, bạn sẽ nhận được email với hướng dẫn đặt lại mật khẩu (nếu email tồn tại trong hệ thống).

              .d-grid
                button.btn.btn-primary.btn-lg(type="submit")
                  i.fa-solid.fa-paper-plane.me-2
                  | Gửi liên kết khôi phục
